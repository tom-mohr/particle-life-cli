version="1.0.1"

root="dist/deb"

./build

if [ -d "$root" ]; then
    rm -r "$root"
fi

pkg="$root"/particle-life-cli-"$version"

mkdir -p "$pkg"
cp dist/linux/* "$pkg"

mkdir "$pkg"/debian/
cp -R debian/* "$pkg"/debian/

cd "$pkg"
debuild -b -sa
